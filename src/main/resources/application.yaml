spring:
  application:
    name: money-hunter
  profiles:
    default: local
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
  datasource:
    url: ${SPRING_DATASOURCE_URL:}
    username: ${SPRING_DATASOURCE_USERNAME:}
    password: ${SPRING_DATASOURCE_PASSWORD:}
    hikari:
      maximum-pool-size: ${SPRING_DATASOURCE_MAX_POOL_SIZE:5}
  flyway:
    enabled: true

server:
  port: ${PORT:8080}

money-hunter:
  app:
    review-tools-enabled: true
    guest-user-enabled: true
    mock-monetization-enabled: true
    apps-in-toss-app-name: gold-hunter
    integration-mode: mock
    distribution-target: TOSS
    toss-login-enabled: false
    toss-user-key-enabled: false
    real-reward-ads-enabled: false
    real-banner-ads-enabled: false
    real-payments-enabled: false
    real-toss-point-rewards-enabled: false
    real-smart-message-enabled: false
    real-share-reward-enabled: false
  admin:
    username: ${MONEY_HUNTER_ADMIN_USERNAME:}
    password-bcrypt: ${MONEY_HUNTER_ADMIN_PASSWORD_BCRYPT:}
    password: ${MONEY_HUNTER_ADMIN_PASSWORD:}
    allow-plain-password: ${MONEY_HUNTER_ADMIN_ALLOW_PLAIN_PASSWORD:false}
  toss-api:
    base-url: ${TOSS_API_BASE_URL:https://apps-in-toss-api.toss.im}
    mtls-certificate-pem: ${TOSS_API_MTLS_CERTIFICATE_PEM:}
    mtls-private-key-pem: ${TOSS_API_MTLS_PRIVATE_KEY_PEM:}
    mtls-certificate-path: ${TOSS_API_MTLS_CERTIFICATE_PATH:}
    mtls-private-key-path: ${TOSS_API_MTLS_PRIVATE_KEY_PATH:}
    connect-timeout: 3s
    request-timeout: 5s
  ads:
    mode: ${TOSS_AD_MODE:test}
    auto-hunt-reward-ad-group-id: ${TOSS_AD_GROUP_AUTO_HUNT_ID:}
    boost-reward-ad-group-id: ${TOSS_AD_GROUP_BOOST_ID:}
    skill-point-reward-ad-group-id: ${TOSS_AD_GROUP_SKILL_POINT_ID:}
    reward-claim-reward-ad-group-id: ${TOSS_AD_GROUP_REWARD_CLAIM_ID:}
    job-change-interstitial-ad-group-id: ${TOSS_AD_GROUP_JOB_CHANGE_ID:}
    banner-ad-group-id: ${TOSS_AD_GROUP_BANNER_ID:}
  iap:
    flare-pet-product-id: ${TOSS_IAP_FLARE_PET_PRODUCT_ID:ait.0000036679.fce5f961.7495b5547b.0055752729}
    aqua-pet-product-id: ${TOSS_IAP_AQUA_PET_PRODUCT_ID:ait.0000036679.979b9313.997baa9d6e.0055860556}
    skill-point-pack-product-id: ${TOSS_IAP_SKILL_POINT_PACK_PRODUCT_ID:ait.0000036679.5d254d7f.73c4076553.0056169217}
  share-reward:
    module-id: ${TOSS_SHARE_REWARD_MODULE_ID:2060c79f-606b-4839-9a86-32294fa74d86}
    message: ${TOSS_SHARE_REWARD_MESSAGE:친구에게 공유하고 SP 5개를 받아요}
  smart-message:
    auto-hunt-ended-template-set-code: ${TOSS_AUTO_HUNT_ENDED_TEMPLATE_SET_CODE:money_hunter_auto_hunt_ended}
    landing-url: ${TOSS_AUTO_HUNT_ENDED_LANDING_URL:intoss://gold-hunter}
  security:
    cors:
      allowed-origin-patterns:
        - https://gold-hunter.apps.tossmini.com
        - https://gold-hunter.private-apps.tossmini.com
        - https://money-hunter-prod-*.a.run.app
        - https://money-hunter-review-*.a.run.app
        - http://localhost:*
        - http://127.0.0.1:*
  economy:
    ad-revenue-per-reward-ad-won: 60
    gold-per-toss-point: 100
    companion-price-won: 4900
    skill-point-pack-price-won: 999
    skill-point-pack-amount: 10
    friend-invite-reward-skill-points: 5
    friend-invite-limit: 3
    max-character-slots: 3
    auto-hunt-ad-seconds: 3600
    boost-ad-seconds: 3600
    max-ad-seconds: 14400
    reward-gold-threshold: 1000
    reward-point-amount: 10

---
spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:15432/money_hunter_local}
    username: ${SPRING_DATASOURCE_USERNAME:money_hunter}
    password: ${SPRING_DATASOURCE_PASSWORD:money_hunter}

---
spring:
  config:
    activate:
      on-profile: prod

money-hunter:
  app:
    review-tools-enabled: false
    guest-user-enabled: false
    mock-monetization-enabled: false
    integration-mode: ${MONEY_HUNTER_INTEGRATION_MODE:toss-production}
    distribution-target: TOSS
    toss-login-enabled: ${TOSS_LOGIN_ENABLED:false}
    toss-user-key-enabled: ${TOSS_USER_KEY_ENABLED:false}
    real-reward-ads-enabled: ${TOSS_REWARD_ADS_ENABLED:false}
    real-banner-ads-enabled: ${TOSS_BANNER_ADS_ENABLED:false}
    real-payments-enabled: ${TOSS_PAYMENTS_ENABLED:false}
    real-toss-point-rewards-enabled: ${TOSS_POINT_REWARDS_ENABLED:false}
    real-smart-message-enabled: ${TOSS_SMART_MESSAGE_ENABLED:false}
    real-share-reward-enabled: ${TOSS_SHARE_REWARD_ENABLED:false}
  security:
    cors:
      allowed-origin-patterns:
        - https://gold-hunter.apps.tossmini.com
        - https://gold-hunter.private-apps.tossmini.com
        - https://money-hunter-prod-*.a.run.app

---
spring:
  config:
    activate:
      on-profile: review

money-hunter:
  app:
    review-tools-enabled: true
    guest-user-enabled: true
    mock-monetization-enabled: true
    integration-mode: review-mock
    distribution-target: TOSS
  security:
    cors:
      allowed-origin-patterns:
        - https://gold-hunter.apps.tossmini.com
        - https://gold-hunter.private-apps.tossmini.com
        - https://money-hunter-review-*.a.run.app
        - http://localhost:*
        - http://127.0.0.1:*

---
spring:
  config:
    activate:
      on-profile: onestore

money-hunter:
  app:
    review-tools-enabled: false
    guest-user-enabled: true
    mock-monetization-enabled: true
    integration-mode: onestore-review
    distribution-target: ONESTORE
